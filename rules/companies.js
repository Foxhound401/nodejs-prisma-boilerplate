const create = {
    rules: {
        "name": "required"
    },
    messages: {
        "required": "Company Name is required"
    }
};

const update = {
    rules: {
        "name": "required"
    },
    messages: {
        "required": "Company Name is required"
    }
}

module.exports = {
    create,
    update
}
